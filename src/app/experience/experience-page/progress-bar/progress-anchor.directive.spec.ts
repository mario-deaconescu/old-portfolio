import { ProgressAnchorDirective } from './progress-anchor.directive';

describe('ProgressAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
